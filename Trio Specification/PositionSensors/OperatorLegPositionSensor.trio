class OperatorLegPositionSensor
    inherits PositionSensor

    visible position;

    temporal domain integer;

    axioms
        -- the legs are in a unique area
        uniqueLeg:
            \forall x \forall y (position(x) && position(y) \implies x == y);
            \exists x (position(x));

end OperationLegPositionSensor.